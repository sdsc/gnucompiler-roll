NAME    = opt-mpc
VERSION = 1.0.2
RELEASE = 0
RPM.EXTRAS         = AutoReq:No

SRC_SUBDIR	= mpc

MPC_NAME	= mpc

# List of *.tar.gz packages that are part of THIS package
TAR_GZ_PKGS = $(MPC_NAME)-$(VERSION).tar.gz

NAME    = opt-mpc
VERSION = 1.0.1
RELEASE = 0
RPM.EXTRAS         = AutoReq:No

SRC_SUBDIR	= mpc

MPC_NAME	= mpc
MPC_VERSION	= 1.0.1

# List of *.tar.gz packages that are part of THIS package
TAR_GZ_PKGS = $(MPC_NAME)-$(MPC_VERSION).tar.gz
